<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_map"
            android:icon="@drawable/ic_menu_map"
            android:title="@string/map"
            android:checked="true"/>
        <item
            android:id="@+id/nav_species"
            android:icon="@drawable/ic_menu_species"
            android:title="@string/species" />
    </group>

    <item android:title="@string/nav_subHeading">
        <menu>
            <item
                android:id="@+id/nav_toggle"
                android:icon="@drawable/ic_menu_gridtoggle"
                android:title="@string/toggle"
                />
            <item
                android:id="@+id/nav_settings"
                android:icon="@drawable/ic_menu_settings"
                android:title="@string/settings" />
        </menu>
    </item>

</menu>
